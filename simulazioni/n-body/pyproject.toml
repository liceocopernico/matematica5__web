[project]
name = "n-body"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matteo Mosangini", email = "mosangini.matteo@liceocopernico.org" }
]
requires-python = ">=3.14"
dependencies = [
    "np>=1.0.2",
    "phalcopulse>=0.1.5",
    "pyvista>=0.48.4",
]

[project.scripts]
n-body = "n_body:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
